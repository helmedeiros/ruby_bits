// CodeMirror v5.1.0 — placeholder stub. The real distribution would be vendored
// from http://codemirror.net/ ; this file is here so the asset pipeline path
// is correct. Replace via:  curl -sL codemirror.net/codemirror-5.1.zip
window.CodeMirror = window.CodeMirror || function(el, opts){ return { getValue: function(){ return el.value; }, setValue: function(v){ el.value = v; } }; };
