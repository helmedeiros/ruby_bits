language: ruby
rvm:
  - 2.2.1
cache: bundler
services:
  - postgresql
  - redis-server
before_script:
  - psql -c 'create database ruby_bits_test;' -U postgres
  - bundle exec rake db:schema:load RAILS_ENV=test
script:
  - bundle exec rubocop
  - bundle exec rspec
notifications:
  email: false
