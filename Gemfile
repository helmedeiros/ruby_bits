source 'https://rubygems.org'

# GitHub Pages gem pins Jekyll to whatever Pages was running in 2015.
# Run `bundle exec jekyll serve` for local preview at http://localhost:4000/ruby_bits/
gem 'github-pages'
